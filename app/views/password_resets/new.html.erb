<h1>Reset Password</h1>

<%= form_tag password_resets_path, :method => :post do %>
	<p>
		<%= label_tag :email %>
		<%= text_field_tag :email, params[:email] %>
	</p>
	 <p><%= submit_tag "Reset Password", :class => "button submit_btn" %></p>
<% end %>
